name: Publish package to npmjs registry
on:
  release:
    types: [published]
jobs:
  publish:
    runs-on: ubuntu-latest
    if: ${{ !endsWith(github.event.release.name, '-alpha')  }}
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org/
          cache: 'npm'
      - run: npm ci
      - name: Update npm
        run: npm install -g npm@latest
      - name: Release
        run: npm publish --provenance --access public
  publish-alpha:
    runs-on: ubuntu-latest
    if: ${{ endsWith(github.event.release.name, '-alpha')  }}
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org/
          cache: 'npm'
      - run: npm ci
      - name: Update npm
        run: npm install -g npm@latest
      - name: Release
        # Without --tag alpha npm moves the latest tag to the prerelease
        run: npm publish --tag alpha --provenance --access public
